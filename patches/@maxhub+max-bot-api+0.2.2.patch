diff --git a/node_modules/@maxhub/max-bot-api/dist/core/helpers/upload.js b/node_modules/@maxhub/max-bot-api/dist/core/helpers/upload.js
--- a/node_modules/@maxhub/max-bot-api/dist/core/helpers/upload.js
+++ b/node_modules/@maxhub/max-bot-api/dist/core/helpers/upload.js
@@ -170,13 +170,17 @@
             return uploadMultipart({ file, uploadUrl }, abortController);
         };
-        this.uploadFromBuffer = async ({ file, uploadUrl, abortController }) => {
+        this.uploadFromBuffer = async ({ file, uploadUrl, abortController, token }) => {
             const formData = new FormData();
             formData.append('data', new Blob([file.buffer]), file.fileName);
             const res = await fetch(uploadUrl, {
                 method: 'POST',
                 body: formData,
                 signal: abortController?.signal,
             });
-            return await res.json();
+            const json = await res.json();
+            // Preserve the token from getUploadUrl if the upload response doesn't include one
+            if (token && !json.token) {
+                json.token = token;
+            }
+            return json;
         };
